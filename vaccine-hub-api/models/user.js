const db = require("./db.js");

class User {
	static async login(username, password) {
		// TODO: post username and password to API endpoint
	}

	static async regster(username, password) {
		//TODO: post username and password to API endpoint
	}
}

module.exports = User;
